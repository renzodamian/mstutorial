server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: cicuit-breaker
          uri: https://github.com/renzodamian/mstutorial
          search-paths: config-data
  application:
    name: config-service